<!DOCTYPE html>
<html>
<head>
  <title>ZFc</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag 'https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css' %>

  <%= hotwire_livereload_tags if Rails.env.development? %>

  <script src="https://cdn.jsdelivr.net/npm/@heroicons/react/solid@1.0.6/dist/index.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@heroicons/react/outline@1.0.6/dist/index.js"></script>
</head>

<body class='bg-gray-300 dark:bg-gray-800'>
  <%= render 'layouts/navbar' %>
  <main class="container mx-auto min-h-screen px-5 flex flex-col">
    <%= yield %>
  </main>

  <script>
    function toggleDarkMode() {
      const isDarkMode = document.body.classList.toggle('dark');
      document.cookie = `dark_mode=${isDarkMode};path=/;max-age=31536000`;
      document.getElementById('sunIcon').classList.toggle('hidden');
      document.getElementById('moonIcon').classList.toggle('hidden');
    }

    if (document.cookie.split(';').some((item) => item.trim().startsWith('dark_mode=true'))) {
      document.body.classList.add('dark');
    }
  </script>

  <%= javascript_include_tag "application", "data-turbo-track": "reload" %>
</body>
</html>
